/*create table test
(
	id_ uniqueidentifier,
	str_ varchar(50),
	int_ int,
)

insert into test
values (NEWID(), 'xia', 1)
insert into test
values (NEWID(), 'b', 2)
insert into test
values (NEWID(), 'c', 3)


create proc test_test
@p_str char(),

as
declare @pwdtem varchar

begin

	if @p_str = null
	begin
		select * from test
	end
	else
	begin
		select * from test
		where str_ = @p_str
	end
end
*/



create proc boolreturn2
as
declare @re bit
begin
	set @re = 1
	return @re
end


create proc boolreturn2
as
begin
	return 1
end